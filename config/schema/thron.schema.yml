thron.settings:
  type: config_object
  label: 'THRON configuration'
  mapping:
    client_id:
      type: text
      label: 'Client ID'
    app_id:
      type: text
      label: 'App ID'
    app_key:
      type: text
      label: 'App key'

media.source.thron:
  type: media.source.field_aware
  label: 'THRON media type configuration'

entity_browser.browser.widget.thron_search:
  type: mapping
  label: 'THRON search widget configuration'
  mapping:
    submit_text:
      type: string
      label: 'Submit button text'
    items_per_page:
      type: integer
      label: 'Number of items to display per page'
    media_type:
      type: string
      label: 'Media type for created entities'